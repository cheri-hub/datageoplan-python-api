# Gov.br Auth API - Configuração de Ambiente
# Copie para .env e ajuste os valores

# ============== Ambiente ==============
ENVIRONMENT=development
DEBUG=true
LOG_LEVEL=INFO
LOG_FORMAT=console

# ============== Servidor ==============
HOST=0.0.0.0
PORT=8000

# ============== CORS ==============
# Origens permitidas (separadas por vírgula)
# Em produção, especificar domínios explicitamente
CORS_ORIGINS=http://localhost:3000,http://localhost:8080

# ============== Segurança ==============
# API Key para autenticação Bearer (OBRIGATÓRIO MUDAR EM PRODUÇÃO!)
# Gerar: openssl rand -hex 32
# Mínimo 32 caracteres, sem padrões "dev-", "test-", "change-"
API_KEY=dev-api-key-change-in-production-min-32-chars-required

# Secret key para JWT/sessions (OBRIGATÓRIO MUDAR EM PRODUÇÃO!)
# Gerar: openssl rand -hex 32
# Mínimo 32 caracteres
SECRET_KEY=change-this-secret-key-in-production-min-32-chars-required

# ============== Diretórios ==============
# Deixe em branco para usar padrões
# DATA_DIR=./data
# SESSIONS_DIR=./data/sessions
# DOWNLOADS_DIR=./data/downloads
# LOGS_DIR=./data/logs

# ============== Gov.br ==============
# GOVBR_LOGIN_URL=https://sso.acesso.gov.br/login
# GOVBR_REDIRECT_URL=https://sigef.incra.gov.br

# ============== SIGEF ==============
# SIGEF_BASE_URL=https://sigef.incra.gov.br
